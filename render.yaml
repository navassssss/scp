services:
  - type: web
    name: kkstories-kindle
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -b 0.0.0.0:10000 --timeout 120 --worker-class gevent app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
